package settings

import (
	"net/url"
	"time"
)

// AerospikeSettings configures Aerospike database connections for UTXO storage.
type AerospikeSettings struct {
	Debug                           bool          `key:"aerospike_debug" desc:"Enable Aerospike debug logging" default:"false" category:"Aerospike" usage:"Verbose logging for Aerospike operations" type:"bool" longdesc:"### Purpose\nEnables verbose debug logging for all Aerospike client operations.\n\n### How It Works\nWhen enabled, the Aerospike client outputs detailed logs including:\n- Connection pool status\n- Cluster discovery and node status changes\n- Operation timing and latencies\n- Retry attempts\n- Error details\n\n### Values\n- **false** (default) - Normal logging level\n- **true** - Verbose debug logging enabled\n\n### Recommendations\n- Enable only during troubleshooting connectivity, performance, or behavior issues\n- **Warning:** Generates significant log volume in production"`
	Host                            string        `key:"aerospike_host" desc:"Aerospike server host" default:"localhost" category:"Aerospike" usage:"Hostname or IP of Aerospike server" type:"string" longdesc:"### Purpose\nSpecifies the hostname or IP address of the Aerospike seed node for initial cluster discovery.\n\n### How It Works\nThe client uses this host to discover all cluster nodes via the Aerospike cluster-aware protocol. In multi-node clusters, specify any single node - the client auto-discovers others. Combined with aerospike_port to form the connection endpoint.\n\n### Format\n**hostname** or **IP address** - DNS names and IP addresses are both supported.\n\n### Recommendations\n- **localhost** (default) - For local development\n- Use a stable cluster node hostname or IP for production deployments"`
	BatchPolicyURL                  *url.URL      `key:"aerospike_batchPolicy" desc:"URL for batch policy configuration" default:"defaultBatchPolicy" category:"Aerospike" usage:"Custom batch operation policy" type:"url" longdesc:"### Purpose\nSpecifies the URL pointing to JSON configuration for Aerospike batch read policy, controlling behavior of multi-key read operations used extensively in UTXO lookups.\n\n### How It Works\nBatch policies control:\n- MaxConcurrentThreads - Parallel batch splits\n- AllowInline - Single-record optimization\n- SendSetName - Include set in digest\n- ReadModeAP - Read consistency level\n\n### Values\n- **defaultBatchPolicy** (default) - Uses Aerospike client defaults\n- Custom URL pointing to JSON configuration file for workload-specific optimization\n\n### Recommendations\n- Use default for most deployments\n- Custom policies can optimize for large batches or specific consistency requirements\n- Used by UTXO store for bulk transaction data fetching"`
	ReadPolicyURL                   *url.URL      `key:"aerospike_readPolicy" desc:"URL for read policy configuration" default:"defaultReadPolicy" category:"Aerospike" usage:"Custom read operation policy" type:"url" longdesc:"### Purpose\nSpecifies the URL pointing to JSON configuration for Aerospike single-record read policy, controlling consistency, timeout, and retry behavior for Get operations.\n\n### How It Works\nRead policies control:\n- ReadModeAP / ReadModeSC - Consistency level\n- TotalTimeout - Overall operation timeout\n- SocketTimeout - Socket-level timeout\n- MaxRetries - Retry attempts\n- SleepBetweenRetries - Delay between retries\n\n### Values\n- **defaultReadPolicy** (default) - Uses client defaults suitable for most workloads\n- Custom URL for specific consistency or performance requirements\n\n### Recommendations\n- **Strong consistency:** Use ReadModeSC=SESSION\n- **Relaxed reads:** Use ReadModeAP=ONE\n- Used for individual UTXO and transaction metadata lookups"`
	WritePolicyURL                  *url.URL      `key:"aerospike_writePolicy" desc:"URL for write policy configuration" default:"defaultWritePolicy" category:"Aerospike" usage:"Custom write operation policy" type:"url" longdesc:"### Purpose\nSpecifies the URL pointing to JSON configuration for Aerospike write policy, controlling durability, timeout, and behavior for Put/Delete operations.\n\n### How It Works\nWrite policies control:\n- CommitLevel - COMMIT_ALL vs COMMIT_MASTER\n- RecordExistsAction - UPDATE, REPLACE, CREATE_ONLY\n- GenerationPolicy - Version control behavior\n- Expiration - Record TTL\n- TotalTimeout - Overall operation timeout\n\n### Values\n- **defaultWritePolicy** (default) - Uses client defaults with COMMIT_ALL for durability\n- Custom URL for write-heavy workloads or relaxed durability requirements\n\n### Recommendations\n- Use default for most deployments requiring strong durability\n- Used for storing new UTXOs and updating transaction metadata"`
	QueryPolicyURL                  *url.URL      `key:"aerospike_queryPolicy" desc:"URL for query policy configuration" default:"defaultQueryPolicy" category:"Aerospike" usage:"Custom query operation policy" type:"url" longdesc:"### Purpose\nSpecifies the URL pointing to JSON configuration for Aerospike query policy, controlling behavior of secondary index queries and scan operations.\n\n### How It Works\nQuery policies control:\n- MaxConcurrentNodes - Parallel node queries\n- RecordQueueSize - Result buffering size\n- IncludeBinData - Return full records vs keys only\n- FailOnClusterChange - Abort on topology changes\n\n### Values\n- **defaultQueryPolicy** (default) - Uses client defaults\n- Custom URL for full-table scans or selective query optimization\n\n### Recommendations\n- Less commonly used in Teranode since most UTXO access is by primary key\n- May be useful for administrative operations or batch processing"`
	Port                            int           `key:"aerospike_port" desc:"Aerospike server port" default:"3000" category:"Aerospike" usage:"Port number for Aerospike connections" type:"int" longdesc:"### Purpose\nSpecifies the TCP port number for Aerospike server connections, combined with aerospike_host to form the complete connection endpoint.\n\n### How It Works\nThe client connects to the specified port on the seed host, then discovers peer ports automatically after initial connection.\n\n### Values\n- **3000** (default) - Standard Aerospike service port\n- 3001 - Mesh heartbeat port\n- 3002 - Fabric (inter-node communication) port\n\n### Recommendations\n- Ensure firewall rules allow TCP traffic on this port\n- In containerized environments, map to the container's exposed port\n- All cluster nodes typically use the same port"`
	UseDefaultBasePolicies          bool          `key:"aerospike_useDefaultBasePolicies" desc:"Use default base policy settings" default:"false" category:"Aerospike" usage:"Apply default timeouts and retries" type:"bool" longdesc:"### Purpose\nControls whether to use the Aerospike client's built-in default base policy for all operations.\n\n### How It Works\nBase policy parameters include:\n- Priority - Operation priority\n- TotalTimeout - Overall timeout\n- SocketTimeout - Socket-level timeout\n- MaxRetries - Retry attempts\n- SleepBetweenRetries - Delay between retries\n- SendKey - Include key in request\n- UseCompression - Enable compression\n\nOperation-specific policies (Read/Write/Batch/Query) override these base settings.\n\n### Values\n- **false** (default) - Teranode sets explicit policy values\n- **true** - Use Aerospike client defaults\n\n### Recommendations\n- Keep disabled to ensure consistent behavior across Aerospike client upgrades\n- Enable only when Aerospike client defaults are specifically preferred"`
	UseDefaultPolicies              bool          `key:"aerospike_useDefaultPolicies" desc:"Use default operation policies" default:"false" category:"Aerospike" usage:"Apply built-in operation policies" type:"bool" longdesc:"### Purpose\nControls whether to use the Aerospike client's built-in default policies for all operation types.\n\n### How It Works\nAffects all operation types:\n- Read operations\n- Write operations\n- Batch operations\n- Query operations\n\n**Important:** Mutually exclusive with custom policy URLs - if enabled, policy URLs are ignored.\n\n### Values\n- **false** (default) - Teranode configures custom policies via policy URL settings\n- **true** - Use Aerospike client defaults for all operations\n\n### Recommendations\n- Keep disabled for production to maintain explicit control over operation behavior\n- Enable for testing environments or when Aerospike client defaults are preferred"`
	WarmUp                          bool          `key:"aerospike_warmUp" desc:"Warm up connection pool on startup" default:"true" category:"Aerospike" usage:"Pre-establish connections at initialization" type:"bool" longdesc:"### Purpose\nControls whether to pre-establish connections to the Aerospike cluster during client initialization.\n\n### How It Works\nWhen enabled, warmup performs:\n- Creates connections up to ConnectionQueueSize per node\n- Performs cluster discovery\n- Validates connectivity\n- Populates connection pool\n\nStartup fails if warmup cannot connect, providing early failure detection.\n\n### Values\n- **true** (default) - Pre-establish connections at startup\n- **false** - Lazy connection establishment\n\n### Recommendations\n- Enable for production to ensure cluster availability before accepting traffic\n- Disable for faster startup in development or when Aerospike is not immediately needed"`
	StoreBatcherDuration            time.Duration `key:"aerospike_storeBatcherDuration" desc:"Batch collection duration for store operations" default:"10ms" category:"Aerospike" usage:"Time window for batching writes" type:"duration" longdesc:"### Purpose\nSets the duration to collect individual write operations before sending as a batch to Aerospike.\n\n### How It Works\nAccumulates writes for up to the specified duration then flushes as a single batch operation. Works with batcher size limits - flushes when **either** duration or size threshold is reached first. Used by UTXO store for efficient bulk writes.\n\n### Trade-offs\n| Setting | Benefit | Drawback |\n|---------|---------|----------|\n| Lower (1-5ms) | Reduced latency | Higher operation count |\n| Higher (20-50ms) | Improved throughput | Added latency |\n\n### Recommendations\n- **10ms** (default) - Balanced for most workloads\n- Use 1-5ms for low-latency requirements\n- Use 20-50ms for high-throughput scenarios"`
	StatsRefreshDuration            time.Duration `key:"aerospike_statsRefresh" desc:"Statistics refresh interval" default:"5s" category:"Aerospike" usage:"How often to refresh cluster statistics" type:"duration" longdesc:"### Purpose\nSets the interval for refreshing Aerospike cluster statistics and metrics.\n\n### How It Works\nPeriodically collects and exposes metrics including:\n- Connection pool utilization\n- Node status\n- Operation latencies\n- Error counts\n- Memory usage\n\nStatistics are exposed via Prometheus metrics for monitoring dashboards, including per-node and aggregate cluster metrics.\n\n### Trade-offs\n| Setting | Benefit | Drawback |\n|---------|---------|----------|\n| Lower | More current data | Higher overhead |\n| Higher | Reduced overhead | May miss transient issues |\n\n### Recommendations\n- **5s** (default) - Good balance for production monitoring\n- Critical for health checks and alerting to detect cluster problems before they impact service"`
	EnableSpendFilterExpressions    bool          `key:"aerospike_enable_spend_filter_expressions" desc:"Enable filter expressions for spend operations" default:"false" category:"Aerospike" usage:"Use server-side filtering for UTXO spends" type:"bool" longdesc:"### Purpose\nControls whether to use Aerospike filter expressions for spend operations, enabling server-side filtering of records.\n\n### How It Works\nFilter expressions allow server-side filtering during read/update operations, reducing network transfer and client processing. Expressions evaluate on the server, returning only matching records.\n\n**Requirement:** Aerospike Enterprise Edition 4.7+ for full expression support.\n\n### Applicability\nOnly takes effect when utxoBatchSize == 1 (one UTXO per record); with larger batch sizes the spend always falls back to the Lua UDF.\n\n### First-seen safety\nThe spend filter asserts the target element is still the unspent 32-byte UTXO hash before writing, so an already-spent UTXO can never be overwritten through this path. A second (double-spend) spender is filtered out and re-evaluated by the Lua UDF, which returns the correctly classified ErrSpent — first-seen is enforced identically to the Lua and SQL paths.\n\n### Values\n- **false** (default) - Uses the Lua UDF spend path\n- **true** - Uses server-side filter expressions (utxoBatchSize == 1 only), with FILTERED_OUT records retried through the Lua UDF\n\n### Recommendations\n- Enable for reduced spend latency on single-UTXO-per-record deployments where filter-outs are rare\n- Filtered-out spends (already-spent, idempotent re-spend, or UtxoSpendableIn set) pay the expression round-trip plus a follow-up Lua batch, so the latency benefit erodes when filter-outs are common\n- **Test in staging** before enabling in production"`
	EnableSetMinedFilterExpressions bool          `key:"aerospike_enable_setmined_filter_expressions" desc:"Enable filter expressions for set mined operations" default:"false" category:"Aerospike" usage:"Use server-side filtering for mined status updates" type:"bool" longdesc:"### Purpose\nControls whether to use Aerospike filter expressions for set-mined operations that mark transactions as included in blocks.\n\n### How It Works\nSimilar to EnableSpendFilterExpressions but for the mined status update path. Works with UseSeparateUDFMinedModule for additional optimization. Server-side expressions reduce round-trips and network transfer.\n\n**Requirement:** Aerospike Enterprise Edition 4.7+\n\n### Values\n- **false** (default) - Uses traditional approach\n- **true** - Uses server-side filter expressions\n\n### Recommendations\n- Enable for performance improvement when updating large batches of transaction mined status\n- **Evaluate impact on write latency** before enabling in production"`
	EnablePreserveFilterExpressions bool          `key:"aerospike_enable_preserve_filter_expressions" desc:"Enable filter expressions for preserve operations" default:"false" category:"Aerospike" usage:"Use server-side batch writes instead of Lua UDF for PreserveTransactions" type:"bool" longdesc:"### Purpose\nControls whether to use Aerospike batch write operations instead of Lua UDFs for PreserveTransactions.\n\n### How It Works\nReplaces the Lua UDF-based preserveUntil call with a BatchWrite using PutOp operations and UPDATE_ONLY policy. Missing records are treated as no-ops. The operation sets the preserveUntil bin and clears deleteAtHeight atomically.\n\n### Values\n- **false** (default) - Uses Lua UDF for preserve operations\n- **true** - Uses batch write operations\n\n### Recommendations\n- Enable to eliminate Lua UDF overhead for preserve operations\n- Safe to enable — the operation is simple and idempotent\n- **Test in staging** before enabling in production"`
	UseSeparateUDFMinedModule       bool          `key:"aerospike_use_separate_udf_mined_module" desc:"Use separate UDF module for mined operations" default:"false" category:"Aerospike" usage:"Isolate mined status UDFs" type:"bool" longdesc:"### Purpose\nControls whether to use a separate User Defined Function (UDF) module for mined status operations.\n\n### How It Works\nUDFs are Lua scripts that run on Aerospike server for complex atomic operations. Separation provides:\n- Isolated mined status logic\n- Independent updates capability\n- Reduced risk of UDF changes affecting other operations\n- Modules can be version-controlled and deployed independently\n\n**Requirement:** Requires UDF module deployment to Aerospike cluster.\n\n### Values\n- **false** (default) - Uses shared UDF module\n- **true** - Uses separate UDF module for mined operations\n\n### Recommendations\n- Enable in large deployments where mined status updates are performance-critical\n- Useful when mined operations need different optimization than other UTXO operations\n\n### Interaction with aerospike_use_native_teranode_ops\nSilently **ignored** when native ops are active. The native operate-path bypasses the UDF executor entirely, so there is no per-Lua-module lock for this setting to spread contention across."`
	SeparateSpendUDFModuleCount     int           `key:"aerospike_separate_udf_spend_module_count" desc:"Number of separate UDF modules for spend operations" default:"0" category:"Aerospike" usage:"Distribute spend operations across UDF modules" type:"int" longdesc:"### Purpose\nControls the number of separate UDF modules to distribute spend operations across for reducing contention.\n\n### How It Works\nEach module handles 1/N of spend operations based on consistent hashing. Reduces UDF contention in high-throughput scenarios where spend operations are the bottleneck.\n\n**Requirement:** Requires deploying multiple UDF modules to Aerospike cluster.\n\n### Values\n- **0** (default) - Uses single shared module\n- **N > 0** - Spreads spend operations across N UDF modules (e.g., 4, 8)\n\n### Trade-offs\n| Setting | Benefit | Drawback |\n|---------|---------|----------|\n| Higher | Reduced per-module contention | Increased deployment complexity |\n| Lower | Simpler deployment | Potential contention bottleneck |\n\n### Recommendations\n- Monitor UDF execution times to determine if module sharding provides benefit\n- Typically only needed for extremely high transaction volumes\n\n### Interaction with aerospike_use_native_teranode_ops\nSilently **ignored** when native ops are active. The native operate-path bypasses the UDF executor entirely, so there is no per-Lua-module lock for this setting to spread contention across."`
	UseNativeTeranodeOps            bool          `key:"aerospike_use_native_teranode_ops" desc:"Use native operate-path for mod-teranode functions instead of UDF" default:"false" category:"Aerospike" usage:"Replace BatchUDF teranode calls with TeranodeModifyOp (wire op type 200)" type:"bool" longdesc:"### Purpose\nControls whether mod-teranode functions (spend, setMined, freeze, ...) are invoked via the native operate-path (TeranodeModifyOp, wire op type 200) instead of the legacy UDF path.\n\n### How It Works\nWhen enabled, the store probes the cluster at startup: it confirms the BSV fork of aerospike-server dispatches the new opcode (a setLocked round-trip whose mutation is read back), then proves spend semantics on a synthetic single-UTXO record — a native spendMulti must succeed and a re-spend with different SpendingData must be rejected with SPENT. Only if both stages pass do teranode invocations (except unspend) route through the native path. If the setting is disabled or any probe stage fails, calls fall back transparently to the UDF path.\n\n### Unspend safety (#899)\nUnspend is always handled by the UDF/Lua path regardless of this setting. The UDF path enforces the #766 SpendingData ownership check before reversing a spend; the native subOpUnspend dispatcher's enforcement cannot be verified from the client, so unspend is fenced off the native path to avoid a silent spend-reversal regression. Enabling this setting therefore does not require any particular unspend-dispatcher server build.\n\n**Requirement:** Requires the BSV fork of aerospike-server (with native operate-path support, or merged equivalent) on every cluster node.\n\n### Values\n- **false** (default) - Uses the legacy UDF path for all mod-teranode invocations\n- **true** - Uses the native operate-path when both setting and server-capability allow; a runtime PARAMETER_ERROR from a stock node demotes back to the UDF path for the process lifetime\n\n### Cluster-version requirement\nAll aerospike-server nodes must run the BSV fork of aerospike-server before this is enabled. The startup probe validates opcode dispatch (setLocked) and first-seen spend enforcement (spendMulti double-spend rejection); unspend never uses the native path (see Unspend safety above), so no unspend-ownership server build is required. The probe writes to two keys and so samples individual partition masters only — a mixed-version cluster can produce either:\n\n- **False positive**: probe lands on BSV-fork masters, sets useNativeTeranodeOps=true; a subsequent write to a partition whose master is still on stock Aerospike fails once with PARAMETER_ERROR, which permanently demotes the store to the UDF path for the rest of the process (the failed batch surfaces an error; everything after recovers).\n- **False negative**: probe lands on a stock master, useNativeTeranodeOps=false is cached for the Store's lifetime even after the cluster finishes upgrading. Restart teranode after the upgrade to re-probe.\n\n### Recommendations\n- Enable on clusters running the BSV fork to bypass the UDF executor (lower latency, higher concurrency under heavy batch load)\n- Upgrade the aerospike-server cluster fully before flipping this setting; mixed-version clusters are not supported. Keep the setting false until the rolling upgrade completes\n- **Test thoroughly** before enabling in production — native path uses different lock granularity than UDF"`
	EnableClientMetrics             bool          `key:"aerospike_enable_client_metrics" desc:"Enable Aerospike client-side metrics collection" default:"true" category:"Aerospike" usage:"Toggle the client.Stats() polling goroutine (client.EnableMetrics stays disabled pending #1001)" type:"bool" longdesc:"### Purpose\nGates the goroutine that polls client.Stats() into Prometheus.\n\n**Important:** client.EnableMetrics(nil) is intentionally NOT called — it is commented out in util/aerospike.go pending issue #1001 (its per-batch nodeStats updateOrInsert path is quadratic under load). This setting therefore does NOT control the client's per-result-code / connection-pool / latency histograms; those are not collected regardless of this flag. It gates only the stats-polling goroutine.\n\n### How It Works\nWhen enabled, util/aerospike.go runs a goroutine that polls cluster stats at StatsRefreshDuration and reflects them into Prometheus. When disabled, that goroutine does not run; operations still execute normally. The EnableMetrics call stays disabled either way.\n\n### Why disable it\nTo drop the periodic client.Stats() polling overhead on clusters where those polled metrics are not consumed. Note: this does not affect the nodeStats write-lock contention from EnableMetrics — that path is already disabled (see #1001), so this flag cannot relieve it.\n\n### Values\n- **true** (default) - Stats-polling goroutine runs (preserves prior behavior)\n- **false** - Stats-polling goroutine does not run\n\n### Recommendations\n- Keep enabled where the polled cluster-stats metrics are useful\n- Disable to remove the polling goroutine's overhead when those metrics are not consumed\n- Re-enabling client.EnableMetrics is tracked separately in issue #1001"`
	SemaphoreMultiplier             float64       `key:"aerospike_semaphore_multiplier" desc:"Multiplier applied to the uaerospike client's connection-queue-derived semaphore capacity" default:"1.0" category:"Aerospike" usage:"Scale or disable (0) the in-process semaphore that throttles concurrent Aerospike operations" type:"float64" longdesc:"### Purpose\nScales (or disables) the in-process semaphore inside util/uaerospike.Client that caps the number of concurrent Aerospike operations performed by this process. The semaphore exists to keep parallel callers from oversubscribing the aerospike-client-go connection pool; this setting tunes that ceiling without touching the underlying ConnectionQueueSize.\n\n### How It Works\nAt client construction time the queue size derived from the aerospike client policy (or DefaultConnectionQueueSize when no policy supplies one) is multiplied by this value, rounded, and clamped to at least 1. The result becomes the buffer of the connSemaphore channel. acquirePermit blocks on a send into that channel; releasePermit receives from it after the operation completes.\n\n### Values\n- **0** disables the semaphore entirely. All acquirePermit / releasePermit calls become no-ops; concurrency is governed only by the underlying aerospike client's own connection pool. Use when the caller already bounds concurrency upstream (e.g. via a batcher) and the extra parking overhead is undesirable.\n- **1.0** (default) preserves the prior behavior (one semaphore slot per ConnectionQueueSize-derived permit).\n- **<1** over-restricts — useful when sharing the aerospike server with other clients.\n- **>1** raises the cap — only safe when the deployment has been verified to handle more concurrent operations than the default queue size implies.\n\n### Recommendations\n- Leave at the default unless you have profiled the in-process semaphore as a bottleneck, or measured pile-ups on acquirePermit from over-parallel callers.\n- Negative values (and NaN) are treated as 0 (semaphore disabled).\n- The resulting channel buffer is clamped to 1<<20 slots so a runaway multiplier value cannot allocate a multi-GB channel."`
	OverloadRetryMaxElapsed         time.Duration `key:"aerospike_overload_retry_max_elapsed" desc:"Total budget for retrying Aerospike operations rejected with DEVICE_OVERLOAD / MAX_ERROR_RATE" default:"2m" category:"Aerospike" usage:"Retry budget per operation when the Aerospike server is overloaded (0 disables)" type:"duration" longdesc:"### Purpose\nBounds the wrapper-level retry inside util/uaerospike.Client that re-issues operations rejected because the Aerospike server cannot keep up (DEVICE_OVERLOAD) or because the client's local error-rate breaker tripped as a consequence (MAX_ERROR_RATE).\n\n### How It Works\nWhen a wrapped operation (Put, PutBins, Delete, Get, Operate, BatchOperate) fails with an overload result code, the client waits with capped exponential backoff (see aerospike_overload_retry_base_backoff / aerospike_overload_retry_max_backoff), randomised by +/-25% jitter to decorrelate concurrent retries, and re-issues it, until it succeeds, fails with a different error, or this budget is spent — then the overload error propagates to the caller. For BatchOperate only the still-overloaded records are resubmitted; other per-record results are preserved untouched. The connection-semaphore permit is released during each backoff sleep and re-acquired before the retry, so a sleeping retrier does not hold a slot the server isn't using.\n\n### Observability\nBecause the retry happens inside the wrapped call, the per-operation latency stats (Put, Get, Operate, BatchOperate) span the backoff sleeps during overload and can read far above raw server latency; the separate Aerospike OverloadRetry stat isolates the time spent retrying.\n\n### Values\n- **2m** (default) - sized to survive the multi-minute sustained overloads seen during big-output mainnet eras, so the post-exhaustion error path stays rare\n- **0** disables the retry layer entirely; overload errors propagate immediately (prior behavior)\n\n### Recommendations\n- Lower it when upstream callers have tight deadlines: the wrapper has no context and can out-wait a caller's gRPC deadline\n- The permit is released during each backoff sleep, so a long budget no longer starves other callers while a retrier waits"`
	OverloadRetryBaseBackoff        time.Duration `key:"aerospike_overload_retry_base_backoff" desc:"Initial wait between Aerospike overload retries" default:"50ms" category:"Aerospike" usage:"First backoff delay for overload retries" type:"duration" longdesc:"### Purpose\nSets the first wait applied before re-issuing an operation that failed with an overload result code (see aerospike_overload_retry_max_elapsed).\n\n### How It Works\nThe wait doubles after every failed retry, capped at aerospike_overload_retry_max_backoff.\n\n### Values\n- **50ms** (default) - quick first retry for short overload blips\n- Non-positive values fall back to the default\n\n### Recommendations\n- Keep small: the first retry often succeeds once the server's write queue drains"`
	OverloadRetryMaxBackoff         time.Duration `key:"aerospike_overload_retry_max_backoff" desc:"Cap on the exponential backoff between Aerospike overload retries" default:"5s" category:"Aerospike" usage:"Maximum backoff delay for overload retries" type:"duration" longdesc:"### Purpose\nCaps the exponentially growing wait between overload retries (see aerospike_overload_retry_max_elapsed).\n\n### How It Works\nThe backoff starts at aerospike_overload_retry_base_backoff, doubles per attempt, and never exceeds this value. Values below the base backoff are raised to it.\n\n### Values\n- **5s** (default) - balances retry frequency against hammering an overloaded server\n\n### Recommendations\n- Keep well below aerospike_overload_retry_max_elapsed so several attempts fit inside the budget"`
}
